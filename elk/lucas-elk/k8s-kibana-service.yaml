apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: lucas-elk
  labels:
    app: kibana
spec:
  selector:
    app: kibana
  ports:
    - name: http
      port: 5601
      targetPort: 5601
      nodePort: 30239
  type: NodePort
