apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: lucas-elk
  labels:
    app: logstash
spec:
  selector:
    app: logstash
  ports:
    - name: beats
      port: 5044
      targetPort: 5044
    - name: api
      port: 9600
      targetPort: 9600
  type: ClusterIP
