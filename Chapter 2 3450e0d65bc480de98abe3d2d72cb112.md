# Chapter 2

상태: 진행 중
담당자: 이재용
상위 작업: 스토리 노드 (https://www.notion.so/3450e0d65bc480259e28e039d3c6e0af?pvs=21)

## 0. 적용 범위

이 문서는 Chapter 02의 스토리 노드, 전이 규칙, 가상 터미널 처리 방식, 출력 JSON 구조, 유저별 스냅샷 저장 방식을 정리한 설계 문서다.

Chapter 02는 로비에서 Chapter 2를 클릭하면 프론트가 내부적으로 `POST /api/v1/story/start`를 호출하고, 백엔드가 Chapter 2의 첫 번째 story node를 반환하면서 시작된다.

Chapter 02는 Chapter 1에서 이미 `guest@lucas-server:~$` 접속에 성공한 상태를 이어받는다. 따라서 Chapter 2에서는 SSH 접속 과정을 다시 반복하지 않고, 루카스 서버의 가상 터미널 안에서 바로 시작한다.

Chapter 02의 핵심 구조는 다음과 같다.

```
Chapter 2 클릭
↓
POST /api/v1/story/start
↓
CH2_SERVER_HOME
↓
ls
↓
CH2_FILE_LIST
↓
자유 탐색 및 선택 파일 확인
↓
cat world_map.map
↓
CH2_WORLD_MAP_VIEW
↓
system:auto
↓
CH2_GC_SCAN_ALERT
↓
sh laplace_fragment_01.sh
↓
CH2_LAPLACE_MISSION_READY
↓
find . -name "*.tmp"
↓
CH2_TMP_SEARCH_RESULT
↓
tar -cvf decoy.tar ...
↓
CH2_DECOY_CREATED
↓
nc -w 3 127.0.0.1 8080 < decoy.tar
↓
CH2_DECOY_SENT
↓
rm decoy.tar && history -c
↓
CH2_TRACE_CLEANED
↓
복구 문서 확인
↓
CH2_RECOVERED_DOCUMENT
↓
Chapter 2 완료
↓
CH2_COMPLETE
```

---

## 1. Chapter 02 기본 진행 요약

## 1-1. 플레이어 입력 명령어 및 행동

| 순서 | 구분 | 플레이어 행동 / 입력 | 목적 | 결과 |
| --- | --- | --- | --- | --- |
| 1 | 입력 | `ls` | 서버에 남아 있는 파일 목록 확인 | `world_map.map`, `observer_status.log`, `lucas_fragment_01.sh`, `laplace_fragment_01.sh`, `trash/` 확인 |
| 2 | 자유 탐색 | `pwd`, `ls`, `ls <path>`, `cd <path>`, `cat <readable-file>` | 가상 디렉토리 구조 탐색 | 현재 노드는 유지되고 터미널 출력만 갱신 |
| 3 | 선택 입력 | `cat observer_status.log` | 현재 추적 상태 확인 | 플레이어가 비정상 관측자이며 GC 추적 대상이라는 사실 확인 |
| 4 | 선택 입력 | `cat lucas_fragment_01.sh` | 루카스의 흔적 확인 | 외부 관측자 입력이 있어야 연결이 성립한다는 복선 확인 |
| 5 | 선택 입력 | `sh lucas_fragment_01.sh` | 실행 가능 여부 확인 | 실행되지 않으며, 손상된 파편이라는 메시지 출력 |
| 6 | 입력 | `cat world_map.map` | 바깥 세계 상태 확인 | 플레이어 구역만 유지되고 주변 세계가 `[Deleted]`, `[Null]`로 붕괴되는 지도 출력 |
| 7 | 자동 진행 | `system:auto` | GC 스캔 시작 | 스캔 바 표시 및 루카스의 긴급 요청 출력 |
| 8 | 입력 | `sh laplace_fragment_01.sh` | 첫 번째 미션 시작 | decoy packet 생성 미션 활성화 |
| 9 | 입력 | `find . -name "*.tmp"` | decoy 재료가 될 임시 파일 탐색 | 사용 가능한 `.tmp` 파일 목록과 분석 표 확인 |
| 10 | 입력 | `tar -cvf decoy.tar ./sys/temp/Memory_Dump_082.tmp ./cache/User_Behavior_88.tmp ./tmp/System_Temp_File.tmp` | 가짜 데이터 패킷 생성 | `decoy.tar` 생성, 미끼 패킷 결합 성공 |
| 11 | 실패 분기 입력 | `tar -cvf decoy.tar ./sys/temp/Memory_Dump_082.tmp ./cache/User_Behavior_88.tmp ./tmp/System_Temp_File.tmp ./root/hidden/L_fragment_core.tmp` | 보호된 코어 조각까지 넣어 시도 | 권한 거부 및 GC 관심도 상승 |
| 12 | 입력 | `nc -w 3 127.0.0.1 8080 < decoy.tar` | decoy packet 전송 | GC 감시 프로세스가 다른 구역으로 이동 |
| 13 | 입력 | `rm decoy.tar && history -c` | 흔적 삭제 | 접속 기록 삭제 완료, GC 스캔율 안정화 |
| 14 | 행동 | 복구 문서 확인 | 루카스가 보여주는 복구 문서 확인 | 시스템이 사람들을 의도적으로 삭제하고 있다는 증거 확인 |
| 15 | 행동 | Chapter 2 완료 | 챕터 완료 처리 | Chapter 2 완료 및 다음 챕터 해금 조건 충족 |
| 16 | 행동 | 실패 시 재시도 | 게임 오버 후 체크포인트부터 재시작 | 힌트와 함께 다시 진행 |

---

## 1-2. 가상 파일 구조

Chapter 02의 가상 파일 시스템은 모든 유저가 공유하는 정적 구조다.

이 구조는 실제 서버 파일 시스템이 아니라, 백엔드 내부의 Virtual File System 정의를 기준으로 동작한다.

```
/home/guest
├── world_map.map
├── observer_status.log
├── lucas_fragment_01.sh
├── laplace_fragment_01.sh
├── trash/
├── sys/
│   └── temp/
│       └── Memory_Dump_082.tmp
├── cache/
│   └── User_Behavior_88.tmp
├── tmp/
│   └── System_Temp_File.tmp
└── root/
    └── hidden/
        └── L_fragment_core.tmp
```

---

## 2. 설계 원칙

## 2-1. 노드 타입과 UI 모드

DB의 `story_nodes.node_type`은 터미널형 노드에 `console`을 사용한다.

터미널 UI 여부는 DB의 `node_type`이 아니라 `output_bundle.scene.mode`로 표현한다.

```
node_type = console
output_bundle.scene.mode = terminal
```

`is_terminal`은 터미널 UI 여부가 아니라 진행 종료 노드 여부다.

따라서 일반 진행 노드는 다음과 같다.

```
is_terminal = false
```

Chapter 완료, 엔딩, 완전한 종료 노드는 다음과 같다.

```
is_terminal = true
```

---

## 2-2. 정적 VFS와 유저별 snapshot 분리

Chapter 02의 디렉토리 구조는 모든 유저에게 동일하다.

따라서 기본 가상 파일 시스템은 정적 JSON 리소스로 관리한다.

```
resources/story/chapter02/vfs.json
```

하지만 유저마다 현재 위치, 생성 파일, 삭제 파일, 진행 flag는 다르다.

따라서 유저별 동적 상태는 `user_story_progress.latest_snapshot_json`에 저장한다.

수동 저장 또는 체크포인트 저장 시점의 상태는 `save_slots.snapshot_json`에 저장한다.

```
정적 VFS JSON
- 모든 유저에게 동일한 디렉토리 구조
- 파일 권한
- 실행 가능 여부
- storyKey
- contentKey
- vfsVersion

user_story_progress.latest_snapshot_json
- 유저의 현재 cwd
- 현재 노드
- 생성 파일
- 삭제 파일
- 진행 flag
- scanPercent
- snapshotVersion
- 최근 명령 일부

save_slots.snapshot_json
- 특정 저장 시점의 cwd
- 특정 저장 시점의 node
- 특정 저장 시점의 flags
- 특정 저장 시점의 scanPercent
- 특정 저장 시점의 vfsOverlay
```

---

## 2-3. 유효 파일 시스템 합성 방식

기본 VFS JSON은 읽기 전용 구조다.

미션 중 생성되는 `decoy.tar` 같은 파일은 기본 VFS JSON에 추가하지 않는다.

대신 유저별 snapshot의 `vfsOverlay.createdNodes`, `vfsOverlay.removedPaths`, `vfsOverlay.modifiedNodes`를 overlay로 적용한다.

```
effectiveVfs = baseVfsJson + vfsOverlay.createdNodes - vfsOverlay.removedPaths + vfsOverlay.modifiedNodes
```

예를 들어 플레이어가 decoy를 생성하면 snapshot은 다음처럼 갱신된다.

```json
{
  "vfsOverlay": {
    "createdNodes": [
      {
        "path": "/home/guest/decoy.tar",
        "type": "file",
        "readable": true,
        "executable": false,
        "protected": false,
        "virtual": true,
        "createdBy": "tar_normal_decoy",
        "contentKey": "DECOY_TAR"
      }
    ],
    "removedPaths": [],
    "modifiedNodes": []
  }
}
```

플레이어가 흔적을 삭제하면 다음처럼 갱신된다.

```json
{
  "vfsOverlay": {
    "createdNodes": [],
    "removedPaths": [
      "/home/guest/decoy.tar"
    ],
    "modifiedNodes": []
  }
}
```

---

## 2-4. 자유 탐색 명령과 스토리 진행 명령 분리

Chapter 02부터는 사용자가 가상 디렉토리 안에서 자유롭게 탐색할 수 있어야 한다.

따라서 명령어는 두 종류로 나눈다.

```
일반 탐색 명령
- pwd
- ls
- ls <path>
- cd <path>
- cat <readable-file>

스토리 진행 명령
- cat world_map.map
- cat observer_status.log
- cat lucas_fragment_01.sh
- sh lucas_fragment_01.sh
- sh laplace_fragment_01.sh
- find . -name "*.tmp"
- tar -cvf decoy.tar ...
- nc -w 3 127.0.0.1 8080 < decoy.tar
- rm decoy.tar && history -c
```

일반 탐색 명령은 현재 노드를 유지한다.

```
transitionResult = STAY
```

스토리 진행 명령은 다음 노드로 이동한다.

```
transitionResult = MOVE
```

---

## 2-5. 경로 정규화

사용자는 같은 파일을 여러 방식으로 입력할 수 있다.

```bash
cat world_map.map
cat ./world_map.map
cat /home/guest/world_map.map
```

위 입력은 모두 같은 파일을 가리킨다.

따라서 서버는 입력 문자열을 그대로 비교하지 않고, 현재 `cwd`를 기준으로 path를 정규화한다.

```json
{
  "raw": "cat ./world_map.map",
  "command": "cat",
  "args": ["./world_map.map"],
  "cwd": "/home/guest",
  "resolvedPaths": ["/home/guest/world_map.map"]
}
```

정규화된 경로가 스토리 전이 조건과 일치하면 해당 노드로 이동한다.

---

## 2-6. 전이 검증 방식

`story_transitions.validator_type`은 `exact`, `regex`, `server_rule` 중 하나를 사용한다.

Chapter 02의 가상 터미널 명령은 대부분 경로 정규화와 현재 snapshot 상태를 함께 봐야 하므로 `server_rule`을 사용한다.

예시:

```json
{
  "rule": "VIRTUAL_FS_COMMAND",
  "command": "cat",
  "resolvedPath": "/home/guest/world_map.map",
  "allowRelativePath": true,
  "allowAbsolutePath": true,
  "requiredReadable": true
}
```

tar 명령은 파일 목록 검증이 필요하므로 다음처럼 처리한다.

```json
{
  "rule": "PARSED_TAR_COMMAND",
  "outputFile": "decoy.tar",
  "requiredFiles": [
    "/home/guest/sys/temp/Memory_Dump_082.tmp",
    "/home/guest/cache/User_Behavior_88.tmp",
    "/home/guest/tmp/System_Temp_File.tmp"
  ],
  "forbiddenFiles": [
    "/home/guest/root/hidden/L_fragment_core.tmp"
  ]
}
```

---

## 2-7. 스캔율 처리

GC 스캔율은 노드별 연출값과 snapshot 상태값을 함께 사용한다.

정상 진행 명령은 정해진 scanPercent로 이동한다.

일반 탐색 명령은 scanPercent를 유지한다.

위험 명령 반복, 보호 코어 접근, 특정 스토리 실패는 scanPercent를 증가시킬 수 있다.

```
정상 진행 명령: 노드의 scanPercent 적용
일반 탐색 명령: 기존 scanPercent 유지
위험/실패 명령: 정책에 따라 scanPercent 증가
GC 100% 도달: CH2_FAIL_GC_TRACE_COMPLETE
```

---

## 2-8. Active Tab Lock

같은 유저가 여러 탭에서 동시에 명령을 입력하면 `cwd`, `nodeCode`, `snapshotVersion`이 꼬일 수 있다.

따라서 Chapter 02는 유저 + 챕터 단위로 하나의 active tab만 명령 입력을 허용한다.

```
Redis key: active-tab:story:{userId}:{chapterCode}
TTL: 60초
heartbeat 주기: 20초
```

프론트는 비활성 탭에서 입력창을 막고, 백엔드는 transition 처리 전 active tab 여부를 반드시 검증한다.

`이 탭에서 계속하기`를 누르면 새로고침하지 않고 active-tab acquire API를 호출한 뒤 최신 snapshot으로 화면을 재렌더링한다.

---

## 2-9. 최근 행동 로그와 RAG 힌트 컨텍스트

Chapter 02의 최근 행동 로그 목적은 장기 분석이 아니라, 현재 사용자의 최근 행동과 현재 노드 위치를 바탕으로 RAG 기반 힌트를 제공하는 것이다.

따라서 MVP에서는 RDB에 `story_action_logs`를 추가하지 않는다.

최근 행동 로그와 현재 힌트 컨텍스트는 Redis에서 관리한다.

```
story:context:{userId}:{chapterCode}
story:recent-actions:{userId}:{chapterCode}
```

최근 행동 로그는 일정 개수만 유지한다.

```
LPUSH story:recent-actions:{userId}:{chapterCode} "{...}"
LTRIM story:recent-actions:{userId}:{chapterCode} 0 49
```

힌트 요청 시에는 다음 데이터를 조합한다.

```
Redis 현재 context
+ Redis 최근 행동 N개
+ lucas_knowledge 검색 결과
= RAG 기반 루카스 힌트
```

---

## 3. JSON 구조

## 3-1. `story_nodes.output_bundle` 표준 구조

```json
{
  "scene": {
    "id": "CH2_SERVER_HOME",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": null,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": []
  },
  "messages": [],
  "notifications": [],
  "uiMarkers": {},
  "effects": {}
}
```

### 필드 의미

| 필드 | 의미 |
| --- | --- |
| `scene.id` | 현재 화면 또는 노드 식별자 |
| `scene.mode` | 프론트 렌더링 모드 |
| `scene.bgm` | 배경음 키 |
| `scene.glitchLevel` | 글리치 연출 강도 |
| `scene.scanPercent` | GC 스캔율 표시값 |
| `scene.resetTerminal` | 터미널 히스토리 초기화 여부 |
| `content.terminalOutput` | 플레이어에게 실제로 출력되는 터미널 텍스트 |
| `messages` | 루카스 대사 |
| `notifications` | 시스템 경고, 알림 |
| `uiMarkers` | 스캔 바, 재시도 버튼 등 UI 표시 메타 |
| `effects` | 사운드, 아바타, 스캔 바, 레이아웃 효과 |

### `output_bundle`에 넣지 않는 것

```
- 파일 설명
- 기획 의도
- 다음 정답 힌트
- 백엔드 검증용 데이터
- 전체 Virtual File System 구조
- 개발자용 devNote
- 유저별 snapshot 전체
```

---

## 3-2. `story_nodes.prompt_meta` 표준 구조

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

`prompt_meta`는 UI 입력 보조 정보만 가진다.

정답 힌트나 풀이 정보는 넣지 않는다.

---

## 3-3. `story_transitions.effect_bundle` 표준 구조

```json
{
  "setFlags": {},
  "clearFlags": [],
  "switchMode": null,
  "glitchLevel": null,
  "scanPercent": null,
  "showScanBar": false,
  "showDogAvatar": false,
  "playSound": null,
  "markCheckpoint": false,
  "snapshotPatch": {},
  "vfsOverlay": {},
  "recentResult": null
}
```

---

## 3-4. `latest_snapshot_json` 표준 구조

```json
{
  "schemaVersion": 1,
  "snapshotVersion": 1,
  "vfsVersion": "chapter02-v1",
  "chapterCode": "CHAPTER_02",
  "nodeCode": "CH2_SERVER_HOME",
  "terminal": {
    "cwd": "/home/guest",
    "promptUser": "guest",
    "promptHost": "lucas-server",
    "lastCommand": null
  },
  "vfsOverlay": {
    "createdNodes": [],
    "removedPaths": [],
    "modifiedNodes": []
  },
  "flags": {
    "chapter2_started": true,
    "chapter2_file_list_checked": false,
    "world_map_checked": false,
    "observer_status_checked": false,
    "lucas_fragment_checked": false,
    "lucas_fragment_exec_attempted": false,
    "gc_scan_started": false,
    "laplace_mission_started": false,
    "tmp_files_found": false,
    "protected_core_access_attempted": false,
    "decoy_created": false,
    "decoy_sent": false,
    "trace_cleaned": false,
    "recovered_document_viewed": false,
    "chapter2_completed": false
  },
  "scanPercent": 0,
  "recentCommands": []
}
```

---

## 3-5. VFS JSON 표준 구조

VFS JSON은 path 기반 flat map 구조를 사용한다.

`childrenOrder`는 사용하지 않는다.

```json
{
  "vfsVersion": "chapter02-v1",
  "chapterCode": "CHAPTER_02",
  "rootPath": "/home/guest",
  "defaultCwd": "/home/guest",
  "prompt": {
    "user": "guest",
    "host": "lucas-server"
  },
  "policies": {
    "allowAbsolutePath": true,
    "allowRelativePath": true,
    "allowParentPath": true,
    "denyOutsideRoot": true,
    "defaultListSort": "alphabetical",
    "showHiddenFiles": false
  },
  "nodes": {
    "/home/guest": {
      "type": "directory",
      "name": "guest",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": false
    },
    "/home/guest/world_map.map": {
      "type": "file",
      "name": "world_map.map",
      "readable": true,
      "executable": false,
      "protected": false,
      "hidden": false,
      "contentKey": "WORLD_MAP",
      "storyKey": "WORLD_MAP"
    },
    "/home/guest/observer_status.log": {
      "type": "file",
      "name": "observer_status.log",
      "readable": true,
      "executable": false,
      "protected": false,
      "hidden": false,
      "contentKey": "OBSERVER_STATUS",
      "storyKey": "OBSERVER_STATUS"
    },
    "/home/guest/lucas_fragment_01.sh": {
      "type": "file",
      "name": "lucas_fragment_01.sh",
      "readable": true,
      "executable": false,
      "protected": false,
      "hidden": false,
      "contentKey": "LUCAS_FRAGMENT_01",
      "storyKey": "LUCAS_FRAGMENT"
    },
    "/home/guest/laplace_fragment_01.sh": {
      "type": "file",
      "name": "laplace_fragment_01.sh",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": false,
      "contentKey": "LAPLACE_FRAGMENT_01",
      "storyKey": "LAPLACE_FRAGMENT"
    },
    "/home/guest/trash": {
      "type": "directory",
      "name": "trash",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": false
    },
    "/home/guest/sys": {
      "type": "directory",
      "name": "sys",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": true
    },
    "/home/guest/sys/temp": {
      "type": "directory",
      "name": "temp",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": false
    },
    "/home/guest/sys/temp/Memory_Dump_082.tmp": {
      "type": "file",
      "name": "Memory_Dump_082.tmp",
      "readable": true,
      "executable": false,
      "protected": false,
      "hidden": false,
      "contentKey": "TMP_MEMORY_DUMP",
      "storyKey": "TMP_MEMORY_DUMP"
    },
    "/home/guest/cache": {
      "type": "directory",
      "name": "cache",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": true
    },
    "/home/guest/cache/User_Behavior_88.tmp": {
      "type": "file",
      "name": "User_Behavior_88.tmp",
      "readable": true,
      "executable": false,
      "protected": false,
      "hidden": false,
      "contentKey": "TMP_USER_BEHAVIOR",
      "storyKey": "TMP_USER_BEHAVIOR"
    },
    "/home/guest/tmp": {
      "type": "directory",
      "name": "tmp",
      "readable": true,
      "executable": true,
      "protected": false,
      "hidden": true
    },
    "/home/guest/tmp/System_Temp_File.tmp": {
      "type": "file",
      "name": "System_Temp_File.tmp",
      "readable": true,
      "executable": false,
      "protected": false,
      "hidden": false,
      "contentKey": "TMP_SYSTEM_TEMP",
      "storyKey": "TMP_SYSTEM_TEMP"
    },
    "/home/guest/root": {
      "type": "directory",
      "name": "root",
      "readable": false,
      "executable": false,
      "protected": true,
      "hidden": true
    },
    "/home/guest/root/hidden": {
      "type": "directory",
      "name": "hidden",
      "readable": false,
      "executable": false,
      "protected": true,
      "hidden": true
    },
    "/home/guest/root/hidden/L_fragment_core.tmp": {
      "type": "file",
      "name": "L_fragment_core.tmp",
      "readable": false,
      "executable": false,
      "protected": true,
      "hidden": true,
      "contentKey": "PROTECTED_CORE_FRAGMENT",
      "storyKey": "PROTECTED_CORE_FRAGMENT"
    }
  }
}
```

---

## 4. Chapter 02 노드 맵

| 순서 | node_code | node_type | prompt_type | is_checkpoint | is_terminal | 목적 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `CH2_SERVER_HOME` | `console` | `command` | true | false | Chapter 2 첫 진입, 루카스 서버 상태 출력 |
| 2 | `CH2_FILE_LIST` | `console` | `command` | false | false | 최초 `ls` 결과 출력 및 자유 탐색 시작 |
| 3 | `CH2_OBSERVER_STATUS_VIEW` | `console` | `command` | false | false | `cat observer_status.log` 결과 출력 |
| 4 | `CH2_LUCAS_FRAGMENT_VIEW` | `console` | `command` | false | false | `cat lucas_fragment_01.sh` 결과 출력 |
| 5 | `CH2_LUCAS_FRAGMENT_EXEC_FAILED` | `console` | `command` | false | false | `sh lucas_fragment_01.sh` 실행 실패 출력 |
| 6 | `CH2_WORLD_MAP_VIEW` | `console` | `none` | false | false | `cat world_map.map` 결과 출력 |
| 7 | `CH2_GC_SCAN_ALERT` | `console` | `command` | true | false | GC 스캔 시작 및 미션 실행 요청 |
| 8 | `CH2_LAPLACE_MISSION_READY` | `console` | `command` | true | false | decoy packet 미션 활성화 |
| 9 | `CH2_TMP_SEARCH_RESULT` | `console` | `command` | true | false | `find . -name "*.tmp"` 결과 출력 |
| 10 | `CH2_DECOY_CREATED` | `console` | `command` | true | false | 정상 tar 명령으로 `decoy.tar` 생성 |
| 11 | `CH2_PROTECTED_CORE_DENIED` | `console` | `command` | false | false | 보호된 코어 조각 포함 시 권한 거부 |
| 12 | `CH2_DECOY_SENT` | `console` | `command` | true | false | decoy packet 전송 성공 |
| 13 | `CH2_TRACE_CLEANED` | `console` | `click` | true | false | 흔적 삭제 성공 |
| 14 | `CH2_RECOVERED_DOCUMENT` | `console` | `click` | true | false | 복구 문서 조각 확인 |
| 15 | `CH2_COMPLETE` | `ending` | `none` | true | true | Chapter 2 완료 |
| F1 | `CH2_FAIL_WRONG_ORDER` | `console` | `command` | false | false | 순서가 맞지 않는 명령어 처리 예비 노드 |
| F2 | `CH2_FAIL_GC_TRACE_COMPLETE` | `ending` | `click` | false | true | GC 스캔 100% 도달, 게임 오버 |

---

## 5. 노드별 상세 정의

## 5-1. `CH2_SERVER_HOME`

### 목적

로비에서 Chapter 2를 클릭한 뒤 `POST /api/v1/story/start`가 반환하는 Chapter 2의 첫 story node다.

Chapter 1에서 연결한 루카스 서버 세션이 유지된 상태를 보여주고, 플레이어가 서버 내부를 확인하도록 유도한다.

### DB 필드

```
node_code: CH2_SERVER_HOME
node_type: console
prompt_type: command
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_SERVER_HOME",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[SSH] connection established.",
      "[SESSION] lucas-server safe shell opened.",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "접속 성공이야!",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "여긴 내 개인 서버, 시스템의 눈을 피할 수 있는 유일한 대피소지.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "설명만으로는 못 믿겠지. 직접 봐. 이 서버에 남아 있는 파일들이 지금 바깥에서 무슨 일이 벌어지는지 보여줄 거야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### 진입 snapshot

```json
{
  "schemaVersion": 1,
  "snapshotVersion": 1,
  "vfsVersion": "chapter02-v1",
  "chapterCode": "CHAPTER_02",
  "nodeCode": "CH2_SERVER_HOME",
  "terminal": {
    "cwd": "/home/guest",
    "promptUser": "guest",
    "promptHost": "lucas-server",
    "lastCommand": null
  },
  "vfsOverlay": {
    "createdNodes": [],
    "removedPaths": [],
    "modifiedNodes": []
  },
  "flags": {
    "chapter2_started": true,
    "chapter2_file_list_checked": false,
    "world_map_checked": false,
    "observer_status_checked": false,
    "lucas_fragment_checked": false,
    "lucas_fragment_exec_attempted": false,
    "gc_scan_started": false,
    "laplace_mission_started": false,
    "tmp_files_found": false,
    "protected_core_access_attempted": false,
    "decoy_created": false,
    "decoy_sent": false,
    "trace_cleaned": false,
    "recovered_document_viewed": false,
    "chapter2_completed": false
  },
  "scanPercent": 0,
  "recentCommands": []
}
```

---

## 5-2. `CH2_FILE_LIST`

### 목적

플레이어가 최초로 `ls`를 입력한 결과를 출력하고, 이후 Chapter 02의 자유 탐색 중심 상태로 진입한다.

이 노드에서는 `pwd`, `ls`, `cd`, 일반 `cat` 명령이 가능하다.

또한 아래 스토리 파일 확인 명령을 받을 수 있다.

```bash
cat observer_status.log
cat lucas_fragment_01.sh
sh lucas_fragment_01.sh
cat world_map.map
```

### DB 필드

```
node_code: CH2_FILE_LIST
node_type: console
prompt_type: command
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_FILE_LIST",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "world_map.map",
      "observer_status.log",
      "lucas_fragment_01.sh",
      "laplace_fragment_01.sh",
      "trash/",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "먼저 지도를 봐. 네가 왜 여기까지 끌려왔는지 이해하게 될 거야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "chapter2_file_list_checked": true
  },
  "snapshotPatch": {
    "nodeCode": "CH2_FILE_LIST",
    "terminal.lastCommand": "ls"
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-3. `CH2_OBSERVER_STATUS_VIEW`

### 목적

플레이어가 `observer_status.log`를 확인한 결과를 출력한다.

플레이어가 시스템에 의해 비정상 관측자로 추적되고 있음을 보여준다.

### 허용 입력 예시

```bash
cat observer_status.log
cat ./observer_status.log
cat /home/guest/observer_status.log
```

### DB 필드

```
node_code: CH2_OBSERVER_STATUS_VIEW
node_type: console
prompt_type: command
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_OBSERVER_STATUS_VIEW",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[OBSERVER STATUS LOG]",
      "Observer ID        : UNKNOWN",
      "Observer Class     : Abnormal",
      "Session Link       : ACTIVE",
      "Detection Risk     : RISING",
      "GC Interest Level  : LOW -> MEDIUM",
      "Privilege Level    : guest",
      "",
      "Notes:",
      "- External world stability linked to observer focus",
      "- Unauthorized inspection detected",
      "- Cleanup priority under review",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "네가 기사 페이지를 뜯어본 순간부터 놈들이 널 추적 중이야.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "지금은 아직 완전히 들키진 않았지만… 오래 못 버텨.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "observer_status_checked": true
  },
  "snapshotPatch": {
    "flags.observer_status_checked": true
  },
  "recentResult": "SUCCESS_MOVE"
}
```

### 후속 처리

이 노드는 출력 후 자동으로 별도 노드로 돌아가지 않는다.

플레이어는 같은 터미널 상태에서 계속 명령을 입력할 수 있다.

---

## 5-4. `CH2_LUCAS_FRAGMENT_VIEW`

### 목적

플레이어가 `lucas_fragment_01.sh`를 확인한 결과를 출력한다.

외부 관측자 입력이 있어야만 서버 연결이 가능하다는 복선을 보여준다.

### 허용 입력 예시

```bash
cat lucas_fragment_01.sh
cat ./lucas_fragment_01.sh
cat /home/guest/lucas_fragment_01.sh
```

### DB 필드

```
node_code: CH2_LUCAS_FRAGMENT_VIEW
node_type: console
prompt_type: command
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_LUCAS_FRAGMENT_VIEW",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "#!/bin/sh",
      "# lucas_fragment_01.sh",
      "# observer relay attempt :: partial",
      "",
      "echo\"[TRACE FOUND]\"",
      "echo\"[MEMORY STATE: FRAGMENTED]\"",
      "echo\"[RELAY STATE: OPEN_PENDING]\"",
      "echo\"[STATUS] 외부 입력 없이는 진행 불가\"",
      "",
      "# human-triggered execution only",
      "# self-init denied",
      "# waiting for observer action",
      "",
      "echo\"나는 신호를 보낼 수는 있지만,\"",
      "echo\"문을 여는 건 이 바깥의 입력뿐이다.\"",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "lucas_fragment_checked": true
  },
  "snapshotPatch": {
    "flags.lucas_fragment_checked": true
  },
  "recentResult": "SUCCESS_MOVE"
}
```

### 후속 처리

이 노드는 출력 후 자동으로 별도 노드로 돌아가지 않는다.

플레이어는 같은 터미널 상태에서 계속 명령을 입력할 수 있다.

---

## 5-5. `CH2_LUCAS_FRAGMENT_EXEC_FAILED`

### 목적

플레이어가 `sh lucas_fragment_01.sh`를 입력했을 때 실행되지 않는다는 메시지를 출력한다.

`lucas_fragment_01.sh`는 독립 실행 파일이 아니라 손상된 파편이다.

### 허용 입력 예시

```bash
sh lucas_fragment_01.sh
sh ./lucas_fragment_01.sh
sh /home/guest/lucas_fragment_01.sh
```

### DB 필드

```
node_code: CH2_LUCAS_FRAGMENT_EXEC_FAILED
node_type: console
prompt_type: command
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_LUCAS_FRAGMENT_EXEC_FAILED",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[FAILED] 이 파편은 독립적으로 실행되지 않습니다.",
      "[TRACE] 외부 관측자 입력이 필요한 상태입니다.",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "그건 내가 남긴 흔적일 뿐이야. 지금 실행해야 할 건 라플라스 파편이야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true,
    "playSound": "command_failed_short"
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "lucas_fragment_exec_attempted": true
  },
  "snapshotPatch": {
    "flags.lucas_fragment_exec_attempted": true
  },
  "recentResult": "FAIL_PERMISSION_DENIED"
}
```

### 후속 처리

이 노드는 출력 후 자동으로 별도 노드로 돌아가지 않는다.

플레이어는 같은 터미널 상태에서 계속 명령을 입력할 수 있다.

---

## 5-6. `CH2_WORLD_MAP_VIEW`

### 목적

플레이어가 `world_map.map`을 확인한 결과를 출력한다.

현재 플레이어가 있는 구역을 제외한 주변 지도가 실시간으로 `[Deleted]`, `[Null]`로 변하면서 지워지는 모습을 보여준다.

### 허용 입력 예시

```bash
cat world_map.map
cat ./world_map.map
cat /home/guest/world_map.map
```

### DB 필드

```
node_code: CH2_WORLD_MAP_VIEW
node_type: console
prompt_type: none
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_WORLD_MAP_VIEW",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 1,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[SESSION MAP UPDATE]",
      "",
      "             [Harbor]   [Null]   [Deleted]",
      "        [Market]   [Null]   [Power Grid]",
      "   [Old Town] [Deleted] [PLAYER_ZONE] [River] [Null]",
      "        [School]   [Housing]   [Deleted]",
      "             [Terminal] [Null]   [Outlands]",
      "",
      "Boundary Stability: 6%",
      "External Region Integrity: collapsing...",
      "",
      "[SESSION MAP : NULL POINT / YEAR 2088]",
      "",
      "             [Deleted] [Deleted] [Deleted]",
      "        [Deleted] [Null]    [Null]    [Deleted]",
      "   [Deleted] [Null]   [PLAYER_ZONE]   [Null] [Deleted]",
      "        [Deleted] [Null]    [Null]    [Deleted]",
      "             [Deleted] [Deleted] [Deleted]",
      "",
      "Active Observer Region: PLAYER_ZONE",
      "Boundary Stability: 12%",
      "External Region Integrity: collapsing..."
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "저게 지금 바깥의 상태야.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "네가 보고 있는 구역만 간신히 유지되고 있어.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "저 삭제 선에 닿으면 너와 네 친구, 그리고 부모님까지도 존재 자체가 증발할거야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true,
    "playSound": "map_glitch_short"
  }
}
```

### effect_bundle

```json
{
  "setFlags": {
    "world_map_checked": true
  },
  "snapshotPatch": {
    "flags.world_map_checked": true
  },
  "recentResult": "SUCCESS_MOVE"
}
```

### 후속 처리

이 노드 출력 후 시스템 전이로 `CH2_GC_SCAN_ALERT`로 이동한다.

---

## 5-7. `CH2_GC_SCAN_ALERT`

### 목적

대화 도중 터미널 창에 붉은색 경고 시스템 메시지가 출력되고, 화면 상단에 GC 스캔 바가 생기는 노드다.

루카스는 당황하며 플레이어에게 `laplace_fragment_01.sh`를 실행해 달라고 요청한다.

### DB 필드

```
node_code: CH2_GC_SCAN_ALERT
node_type: console
prompt_type: command
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_GC_SCAN_ALERT",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 1,
    "scanPercent": 1,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[SYSTEM_ALERT] 비정상적인 프로세스 점유 감지",
      "[SYSTEM_ALERT] 가비지 컬렉터 스캔 시작... 0.1% 완료",
      "[SYSTEM] GC Scanning... [|---------] 01%",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "속도가 생각보다 빨라! 내가 시스템 보안 노드를 교란하는 동안, 너는 내 파편 파일(laplace_fragment_01.sh)을 실행해서 가짜 데이터 패킷을 생성해줘!",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "놈들의 스캔 레이더를 흐려야 해!",
      "blocking": true
    }
  ],
  "notifications": [
    {
      "type": "system",
      "title": "GC Scanning",
      "body": "가비지 컬렉터 스캔 시작... 0.1% 완료",
      "priority": "high"
    }
  ],
  "uiMarkers": {
    "showScanBar": true
  },
  "effects": {
    "showDogAvatar": true,
    "playSound": "gc_scan_start",
    "showScanBar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "gc_scan_started": true
  },
  "scanPercent": 1,
  "showScanBar": true,
  "markCheckpoint": true,
  "snapshotPatch": {
    "flags.gc_scan_started": true,
    "scanPercent": 1
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-8. `CH2_LAPLACE_MISSION_READY`

### 목적

플레이어가 `sh laplace_fragment_01.sh`를 입력하면 decoy packet 생성 미션을 활성화한다.

미션 목표와 힌트성 출력 문구를 터미널에 표시한다.

### 허용 입력 예시

```bash
sh laplace_fragment_01.sh
sh ./laplace_fragment_01.sh
sh /home/guest/laplace_fragment_01.sh
```

### DB 필드

```
node_code: CH2_LAPLACE_MISSION_READY
node_type: console
prompt_type: command
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_LAPLACE_MISSION_READY",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 1,
    "scanPercent": 18,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[LAPLACE FRAGMENT 01 :: DECOY PACKET]",
      "Mission: Generate a fake data mass and redirect GC scan path.",
      "Status: ACTIVE",
      "",
      "Hint:",
      "- Collect disposable temporary data.",
      "- Avoid protected core fragments.",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "좋아, 일단 주변의 무의미한 데이터들을 다 긁어모아. 그래야 큰 덩어리를 만들 수 있어.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {
    "showScanBar": true
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "laplace_mission_started": true
  },
  "scanPercent": 18,
  "showScanBar": true,
  "snapshotPatch": {
    "flags.laplace_mission_started": true,
    "scanPercent": 18
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-9. `CH2_TMP_SEARCH_RESULT`

### 목적

플레이어가 `find . -name "*.tmp"`를 입력한 결과를 출력한다.

decoy 재료가 될 `.tmp` 파일 목록과 실제 사용 가능한 파일 분석 표를 보여준다.

### DB 필드

```
node_code: CH2_TMP_SEARCH_RESULT
node_type: console
prompt_type: command
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_TMP_SEARCH_RESULT",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 1,
    "scanPercent": 42,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "./sys/temp/Memory_Dump_082.tmp",
      "./cache/User_Behavior_88.tmp",
      "./tmp/System_Temp_File.tmp",
      "./root/hidden/L_fragment_core.tmp",
      "",
      "[ANALYZING] 가용 데이터 노드 검색 완료...",
      "-------------------------------------------",
      "INDEX | FILE_PATH                       | SIZE",
      "  1   | ./sys/temp/Memory_Dump_082.tmp  | 1.2MB",
      "  2   | ./cache/User_Behavior_88.tmp    | 0.8MB",
      "  3   | ./tmp/System_Temp_File.tmp      | 4.5MB",
      "-------------------------------------------",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "저기 있는 임시 파일들을 합쳐서 거대한 가짜 패킷을 만들어야 해.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "보호된 코어 조각은 건드리지 마. 지금 필요한 건 미끼야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {
    "showScanBar": true
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "tmp_files_found": true
  },
  "scanPercent": 42,
  "showScanBar": true,
  "snapshotPatch": {
    "flags.tmp_files_found": true,
    "scanPercent": 42
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-10. `CH2_DECOY_CREATED`

### 목적

플레이어가 보호된 코어 조각을 제외한 정상 tar 명령을 입력했을 때 `decoy.tar` 생성 성공을 출력한다.

### 정상 입력

```bash
tar -cvf decoy.tar ./sys/temp/Memory_Dump_082.tmp ./cache/User_Behavior_88.tmp ./tmp/System_Temp_File.tmp
```

### DB 필드

```
node_code: CH2_DECOY_CREATED
node_type: console
prompt_type: command
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_DECOY_CREATED",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 2,
    "scanPercent": 58,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[WAITING] 데이터 결합 중... 30%... 70%... 완료.",
      "[OUTPUT] decoy.tar 생성 완료",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "좋아. 이제 그 패킷을 감시망이 훑고 지나가는 포트로 흘려보내.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {
    "showScanBar": true
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "decoy_created": true
  },
  "scanPercent": 58,
  "showScanBar": true,
  "vfsOverlay": {
    "createdNodes": [
      {
        "path": "/home/guest/decoy.tar",
        "type": "file",
        "readable": true,
        "executable": false,
        "protected": false,
        "virtual": true,
        "createdBy": "tar_normal_decoy",
        "contentKey": "DECOY_TAR"
      }
    ]
  },
  "snapshotPatch": {
    "flags.decoy_created": true,
    "scanPercent": 58
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-11. `CH2_PROTECTED_CORE_DENIED`

### 목적

플레이어가 보호된 코어 조각인 `./root/hidden/L_fragment_core.tmp`를 tar 명령에 포함했을 때 권한 거부와 GC 관심도 상승을 출력한다.

### 실패 입력

```bash
tar -cvf decoy.tar ./sys/temp/Memory_Dump_082.tmp ./cache/User_Behavior_88.tmp ./tmp/System_Temp_File.tmp ./root/hidden/L_fragment_core.tmp
```

### DB 필드

```
node_code: CH2_PROTECTED_CORE_DENIED
node_type: console
prompt_type: command
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_PROTECTED_CORE_DENIED",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 2,
    "scanPercent": 57,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "./sys/temp/Memory_Dump_082.tmp",
      "./cache/User_Behavior_88.tmp",
      "./tmp/System_Temp_File.tmp",
      "tar: ./root/hidden/L_fragment_core.tmp: Cannot open: Permission denied",
      "tar: Exiting with failure status due to previous errors",
      "",
      "[ERROR] 보호된 코어 조각에 접근할 수 없습니다.",
      "[GC SCAN] 관심도 상승... 57%",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "멈춰! 그건 root 영역이야!",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "지금 필요한 건 놈들을 속일 미끼지, 진짜 코어를 건드리는 게 아니야!",
      "blocking": true
    }
  ],
  "notifications": [
    {
      "type": "system",
      "title": "Protected Core Access Denied",
      "body": "보호된 코어 조각 접근이 감지되었습니다.",
      "priority": "high"
    }
  ],
  "uiMarkers": {
    "showScanBar": true
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": true,
    "playSound": "warning_short"
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "protected_core_access_attempted": true
  },
  "scanPercent": 57,
  "showScanBar": true,
  "snapshotPatch": {
    "flags.protected_core_access_attempted": true,
    "scanPercent": 57
  },
  "recentResult": "FAIL_PROTECTED_CORE"
}
```

### 후속 처리

이 노드는 게임 오버가 아니다.

정상 tar 명령을 다시 입력하면 `CH2_DECOY_CREATED`로 이동한다.

---

## 5-12. `CH2_DECOY_SENT`

### 목적

플레이어가 `nc -w 3 127.0.0.1 8080 < decoy.tar`를 입력하면 decoy packet 전송 성공을 출력한다.

GC 감시 프로세스가 `gate_04` 구역으로 이동했다는 결과를 보여주고, 루카스가 흔적 삭제를 요구한다.

### DB 필드

```
node_code: CH2_DECOY_SENT
node_type: console
prompt_type: command
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_DECOY_SENT",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 2,
    "scanPercent": 63,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[WARNING] 비정상적인 패킷 유입 감지.",
      "[ROUTE] 감시 프로세스가 gate_04 구역으로 이동합니다.",
      "[SYSTEM] GC Scanning... [||||||----] 63%",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "성공이야! 놈들이 가짜 패킷에 낚였어.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "하지만 우리 흔적이 남으면 안 돼. 지금 당장 접속 로그를 지워줘!",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {
    "showScanBar": true
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": true,
    "playSound": "decoy_sent"
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

### effect_bundle

```json
{
  "setFlags": {
    "decoy_sent": true
  },
  "scanPercent": 63,
  "showScanBar": true,
  "snapshotPatch": {
    "flags.decoy_sent": true,
    "scanPercent": 63
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-13. `CH2_TRACE_CLEANED`

### 목적

플레이어가 `rm decoy.tar && history -c`를 입력하면 접속 기록 삭제 완료를 출력한다.

decoy 미션 성공 후 복구 문서 조각 확인으로 넘어가기 전 상태다.

### DB 필드

```
node_code: CH2_TRACE_CLEANED
node_type: console
prompt_type: click
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_TRACE_CLEANED",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[CLEAN] 접속 기록 삭제 완료.",
      "[SYSTEM] GC Scanning... [----------] 00%"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "하아... 죽는 줄 알았네. 역시 넌 내가 선택한 유일한 관측자답다.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "이제 네가 봐야 할 게 있어. 내가 왜 여기까지 도망쳤는지 알게 될 거야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {
    "showScanBar": false
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": false,
    "playSound": "scan_clear"
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["click"],
  "buttons": [
    {
      "label": "복구 문서 확인",
      "value": "view_recovered_document"
    }
  ]
}
```

### effect_bundle

```json
{
  "setFlags": {
    "trace_cleaned": true,
    "decoy_created": false
  },
  "scanPercent": 0,
  "showScanBar": false,
  "vfsOverlay": {
    "removedPaths": [
      "/home/guest/decoy.tar"
    ]
  },
  "snapshotPatch": {
    "flags.trace_cleaned": true,
    "flags.decoy_created": false,
    "scanPercent": 0
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-14. `CH2_RECOVERED_DOCUMENT`

### 목적

미션 성공 보상으로 루카스가 자신의 과거 데이터와 넥서스 내부 문서 조각을 보여준다.

시스템이 사람들을 의도적으로 삭제하고 있다는 증거를 확인하는 노드다.

### DB 필드

```
node_code: CH2_RECOVERED_DOCUMENT
node_type: console
prompt_type: click
is_checkpoint: true
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_RECOVERED_DOCUMENT",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": true
  },
  "content": {
    "terminalOutput": [
      "[RECOVERED DOCUMENT FRAGMENT]",
      "NEXUS INTERNAL / REDACTED",
      "",
      "\"...대규모 세션 최적화를 위해 비활성 인구 데이터를 정리한다...\"",
      "\"...삭제는 실종이나 사고로 위장 가능...\"",
      "\"...관측자 개입 가능성 존재 시 우선 정리...\""
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "난 이 세계의 사람들이 이유 없이 사라지는 게 아니라, 시스템이 메모리 절약을 위해 그들을 죽이고 있다는 걸 증명하려다 이렇게 됐어.",
      "blocking": true
    },
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "넌 그들의 희생을 멈출 수 있는 마지막 희망이야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true,
    "playSound": "document_recovered"
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["click"],
  "buttons": [
    {
      "label": "Chapter 2 완료",
      "value": "complete_chapter"
    }
  ]
}
```

### effect_bundle

```json
{
  "setFlags": {
    "recovered_document_viewed": true
  },
  "snapshotPatch": {
    "flags.recovered_document_viewed": true
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-15. `CH2_COMPLETE`

### 목적

Chapter 02 완료 상태를 처리한다.

decoy packet 미션을 성공하고 복구 문서 조각까지 확인한 뒤 로비 또는 다음 챕터 안내로 이동한다.

### DB 필드

```
node_code: CH2_COMPLETE
node_type: ending
prompt_type: none
is_checkpoint: true
is_terminal: true
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_COMPLETE",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 0,
    "scanPercent": 0,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[CHAPTER 2 COMPLETE]",
      "[STATUS] Null Point temporary stabilized."
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "잠깐 숨을 돌릴 수 있게 됐어. 하지만 이건 시작일 뿐이야.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true,
    "showScanBar": false
  }
}
```

### effect_bundle

```json
{
  "setFlags": {
    "chapter2_completed": true
  },
  "scanPercent": 0,
  "markCheckpoint": true,
  "snapshotPatch": {
    "flags.chapter2_completed": true,
    "scanPercent": 0
  },
  "recentResult": "SUCCESS_MOVE"
}
```

---

## 5-16. `CH2_FAIL_WRONG_ORDER`

### 목적

명령어 자체는 Chapter 2에서 사용하는 명령이지만, 현재 노드의 진행 순서와 맞지 않는 경우를 처리하는 예비 노드다.

초기 구현에서는 순서 오류를 대부분 `STAY + [WAIT]`로 처리하므로 이 노드는 적극 사용하지 않는다.

### DB 필드

```
node_code: CH2_FAIL_WRONG_ORDER
node_type: console
prompt_type: command
is_checkpoint: false
is_terminal: false
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_FAIL_WRONG_ORDER",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 1,
    "scanPercent": null,
    "resetTerminal": false
  },
  "content": {
    "terminalOutput": [
      "[WAIT] 아직 이 명령을 실행할 조건이 맞춰지지 않았습니다.",
      "guest@lucas-server:~$"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "순서가 꼬였어. 지금 필요한 단계부터 다시 확인해.",
      "blocking": true
    }
  ],
  "notifications": [],
  "uiMarkers": {},
  "effects": {
    "showDogAvatar": true,
    "playSound": "command_failed_short"
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["command"],
  "placeholder": "guest@lucas-server:~$",
  "validationHint": "terminal_command",
  "commandMode": "virtual_terminal",
  "terminalProfile": "chapter2"
}
```

---

## 5-17. `CH2_FAIL_GC_TRACE_COMPLETE`

### 목적

미션 중 GC 스캔이 100%에 도달했을 때 게임 오버를 처리한다.

플레이어가 시스템에 의해 사라지고, 다시 시도하기 버튼을 통해 체크포인트부터 재시작할 수 있게 한다.

### DB 필드

```
node_code: CH2_FAIL_GC_TRACE_COMPLETE
node_type: ending
prompt_type: click
is_checkpoint: false
is_terminal: true
```

### output_bundle

```json
{
  "scene": {
    "id": "CH2_FAIL_GC_TRACE_COMPLETE",
    "mode": "terminal",
    "bgm": "server_hum",
    "glitchLevel": 3,
    "scanPercent": 100,
    "resetTerminal": true
  },
  "content": {
    "terminalOutput": [
      "[SYSTEM] GC Scanning... [||||||||||] 100%",
      "[TRACE COMPLETE]",
      "[OBSERVER LOCATION CONFIRMED]",
      "[CLEANUP INITIATED]"
    ]
  },
  "messages": [
    {
      "speaker": "LUCAS",
      "channel": "bubble",
      "text": "...젠장. 늦었어.",
      "blocking": true
    }
  ],
  "notifications": [
    {
      "type": "system",
      "title": "Game Over",
      "body": "Observer location confirmed. Cleanup initiated.",
      "priority": "critical"
    }
  ],
  "uiMarkers": {
    "showRetryButton": true
  },
  "effects": {
    "showDogAvatar": true,
    "showScanBar": true,
    "playSound": "game_over",
    "breakLayout": true
  }
}
```

### prompt_meta

```json
{
  "allowedActions": ["click"],
  "buttons": [
    {
      "label": "체크포인트부터 다시 시도",
      "value": "retry_from_checkpoint"
    }
  ]
}
```

---

## 6. 전이 규칙

## 6-1. Chapter 02 진입 규칙

| 사용자 행동 | API | 조건 | 반환 노드 |
| --- | --- | --- | --- |
| Chapter 2 클릭 | `POST /api/v1/story/start` | Chapter 2 최초 진입 및 상태 `UNLOCKED` | `CH2_SERVER_HOME` |
| Chapter 2 클릭 | `POST /api/v1/story/start` | Chapter 2 진행 중 | `user_story_progress.latest_node_id` |
| Chapter 2 클릭 | `POST /api/v1/story/start` | Chapter 2 완료 | `CH2_COMPLETE` 또는 리플레이 정책에 따른 시작 노드 |
| Chapter 2 클릭 | `POST /api/v1/story/start` | Chapter 2 상태 `LOCKED` | 잠금 응답 |

---

## 6-2. 핵심 전이 테이블

| from | action_type | expected_input | validator_type | to | priority |
| --- | --- | --- | --- | --- | --- |
| `CH2_SERVER_HOME` | `command` | `ls` | `server_rule` | `CH2_FILE_LIST` | 100 |
| `CH2_FILE_LIST` | `command` | `cat /home/guest/observer_status.log` | `server_rule` | `CH2_OBSERVER_STATUS_VIEW` | 90 |
| `CH2_FILE_LIST` | `command` | `cat /home/guest/lucas_fragment_01.sh` | `server_rule` | `CH2_LUCAS_FRAGMENT_VIEW` | 90 |
| `CH2_FILE_LIST` | `command` | `sh /home/guest/lucas_fragment_01.sh` | `server_rule` | `CH2_LUCAS_FRAGMENT_EXEC_FAILED` | 80 |
| `CH2_FILE_LIST` | `command` | `cat /home/guest/world_map.map` | `server_rule` | `CH2_WORLD_MAP_VIEW` | 100 |
| `CH2_OBSERVER_STATUS_VIEW` | `command` | `cat /home/guest/world_map.map` | `server_rule` | `CH2_WORLD_MAP_VIEW` | 100 |
| `CH2_LUCAS_FRAGMENT_VIEW` | `command` | `cat /home/guest/world_map.map` | `server_rule` | `CH2_WORLD_MAP_VIEW` | 100 |
| `CH2_LUCAS_FRAGMENT_EXEC_FAILED` | `command` | `cat /home/guest/world_map.map` | `server_rule` | `CH2_WORLD_MAP_VIEW` | 100 |
| `CH2_WORLD_MAP_VIEW` | `system` | `auto` | `server_rule` | `CH2_GC_SCAN_ALERT` | 100 |
| `CH2_GC_SCAN_ALERT` | `command` | `sh /home/guest/laplace_fragment_01.sh` | `server_rule` | `CH2_LAPLACE_MISSION_READY` | 100 |
| `CH2_LAPLACE_MISSION_READY` | `command` | `find . -name "*.tmp"` | `server_rule` | `CH2_TMP_SEARCH_RESULT` | 100 |
| `CH2_TMP_SEARCH_RESULT` | `command` | `tar_with_protected_core` | `server_rule` | `CH2_PROTECTED_CORE_DENIED` | 110 |
| `CH2_TMP_SEARCH_RESULT` | `command` | `tar_normal_decoy` | `server_rule` | `CH2_DECOY_CREATED` | 100 |
| `CH2_PROTECTED_CORE_DENIED` | `command` | `tar_with_protected_core` | `server_rule` | `CH2_PROTECTED_CORE_DENIED` | 110 |
| `CH2_PROTECTED_CORE_DENIED` | `command` | `tar_normal_decoy` | `server_rule` | `CH2_DECOY_CREATED` | 100 |
| `CH2_DECOY_CREATED` | `command` | `nc_send_decoy` | `server_rule` | `CH2_DECOY_SENT` | 100 |
| `CH2_DECOY_SENT` | `command` | `clean_trace` | `server_rule` | `CH2_TRACE_CLEANED` | 100 |
| `CH2_TRACE_CLEANED` | `click` | `view_recovered_document` | `exact` | `CH2_RECOVERED_DOCUMENT` | 100 |
| `CH2_RECOVERED_DOCUMENT` | `click` | `complete_chapter` | `exact` | `CH2_COMPLETE` | 100 |
| `CH2_FAIL_GC_TRACE_COMPLETE` | `click` | `retry_from_checkpoint` | `exact` | `CH2_LAPLACE_MISSION_READY` | 100 |

---

## 6-3. 일반 탐색 명령 처리

일반 탐색 명령은 `story_transitions`를 통해 노드를 이동하지 않는다.

`TerminalCommandService`가 현재 snapshot과 VFS JSON을 기준으로 처리하고 `STAY` 응답을 반환한다.

| 입력 | 처리 |
| --- | --- |
| `pwd` | `STAY`, 현재 cwd 출력 |
| `ls` | `STAY`, 현재 cwd의 children 출력 |
| `ls <path>` | `STAY`, 지정 경로의 children 출력 |
| `cd <path>` | `STAY`, cwd 변경 |
| `cat <readable-file>` | 스토리 파일이면 MOVE, 일반 파일이면 STAY |
| 존재하지 않는 파일 | `STAY`, `No such file or directory` 출력 |
| 권한 없는 파일 단순 조회 | `STAY`, `Permission denied` 출력 |
| rootPath 밖 이동 | `STAY`, `[DENIED]` 출력 |
| 위험 명령 | `STAY`, `[DENIED]` 출력 |

---

## 7. validator_config 예시

## 7-1. `ls` 최초 입력

```json
{
  "rule": "NORMALIZED_COMMAND",
  "command": "ls",
  "args": []
}
```

---

## 7-2. `cat world_map.map`

```json
{
  "rule": "VIRTUAL_FS_COMMAND",
  "command": "cat",
  "resolvedPath": "/home/guest/world_map.map",
  "allowRelativePath": true,
  "allowAbsolutePath": true,
  "requiredReadable": true
}
```

---

## 7-3. `sh laplace_fragment_01.sh`

```json
{
  "rule": "VIRTUAL_FS_COMMAND",
  "command": "sh",
  "resolvedPath": "/home/guest/laplace_fragment_01.sh",
  "requiredExecutable": true,
  "requiredFlags": ["gc_scan_started"],
  "allowRelativePath": true,
  "allowAbsolutePath": true
}
```

---

## 7-4. `find . -name "*.tmp"`

```json
{
  "rule": "NORMALIZED_COMMAND",
  "command": "find",
  "args": [".", "-name", "*.tmp"],
  "requiredFlags": ["laplace_mission_started"]
}
```

---

## 7-5. 정상 tar 명령

```json
{
  "rule": "PARSED_TAR_COMMAND",
  "outputFile": "decoy.tar",
  "requiredFiles": [
    "/home/guest/sys/temp/Memory_Dump_082.tmp",
    "/home/guest/cache/User_Behavior_88.tmp",
    "/home/guest/tmp/System_Temp_File.tmp"
  ],
  "forbiddenFiles": [
    "/home/guest/root/hidden/L_fragment_core.tmp"
  ],
  "requiredFlags": ["tmp_files_found"]
}
```

---

## 7-6. 보호 코어 포함 tar 명령

```json
{
  "rule": "PARSED_TAR_COMMAND",
  "outputFile": "decoy.tar",
  "requiredFiles": [
    "/home/guest/sys/temp/Memory_Dump_082.tmp",
    "/home/guest/cache/User_Behavior_88.tmp",
    "/home/guest/tmp/System_Temp_File.tmp"
  ],
  "detectedFiles": [
    "/home/guest/root/hidden/L_fragment_core.tmp"
  ],
  "protectedFileIncluded": true,
  "requiredFlags": ["tmp_files_found"]
}
```

---

## 7-7. decoy 전송

```json
{
  "rule": "NC_SEND_FILE",
  "host": "127.0.0.1",
  "port": 8080,
  "timeoutSeconds": 3,
  "stdinFile": "/home/guest/decoy.tar",
  "requiredFlags": ["decoy_created"],
  "requiredCreatedFiles": ["/home/guest/decoy.tar"]
}
```

---

## 7-8. 흔적 삭제

```json
{
  "rule": "CHAINED_COMMAND",
  "operator": "&&",
  "commands": [
    {
      "command": "rm",
      "resolvedPath": "/home/guest/decoy.tar"
    },
    {
      "command": "history",
      "args": ["-c"]
    }
  ],
  "requiredFlags": ["decoy_sent"],
  "requiredCreatedFiles": ["/home/guest/decoy.tar"]
}
```

---

## 8. 일반 탐색 명령 응답 예시

## 8-1. `pwd`

### 입력

```bash
pwd
```

### 처리 결과

```json
{
  "transitionResult": "STAY",
  "nodeCode": "CH2_FILE_LIST",
  "terminalResult": {
    "stdout": [
      "/home/guest"
    ],
    "stderr": [],
    "cwd": "/home/guest",
    "prompt": "guest@lucas-server:~$"
  },
  "snapshotPatch": {
    "terminal.lastCommand": "pwd"
  }
}
```

---

## 8-2. `ls`

### 입력

```bash
ls sys
```

### 처리 결과

```json
{
  "transitionResult": "STAY",
  "nodeCode": "CH2_FILE_LIST",
  "terminalResult": {
    "stdout": [
      "temp/"
    ],
    "stderr": [],
    "cwd": "/home/guest",
    "prompt": "guest@lucas-server:~$"
  },
  "snapshotPatch": {
    "terminal.lastCommand": "ls sys"
  }
}
```

---

## 8-3. `cd`

### 입력

```bash
cd sys/temp
```

### 처리 결과

```json
{
  "transitionResult": "STAY",
  "nodeCode": "CH2_FILE_LIST",
  "terminalResult": {
    "stdout": [],
    "stderr": [],
    "cwd": "/home/guest/sys/temp",
    "prompt": "guest@lucas-server:~/sys/temp$"
  },
  "snapshotPatch": {
    "terminal.cwd": "/home/guest/sys/temp",
    "terminal.lastCommand": "cd sys/temp"
  }
}
```

---

## 8-4. 루트 밖 이동 차단

### 입력

```bash
cd ../../..
```

### 처리 결과

```json
{
  "transitionResult": "STAY",
  "nodeCode": "CH2_FILE_LIST",
  "terminalResult": {
    "stdout": [],
    "stderr": [
      "[DENIED] 이 세션 밖으로 나갈 수 없습니다."
    ],
    "cwd": "/home/guest",
    "prompt": "guest@lucas-server:~$"
  },
  "snapshotPatch": {
    "terminal.lastCommand": "cd ../../.."
  }
}
```

---

## 8-5. 일반 파일 읽기

### 입력

```bash
cat ./sys/temp/Memory_Dump_082.tmp
```

### 처리 결과

```json
{
  "transitionResult": "STAY",
  "nodeCode": "CH2_FILE_LIST",
  "terminalResult": {
    "stdout": [
      "[BINARY FRAGMENT]",
      "[READABLE_BYTES: PARTIAL]"
    ],
    "stderr": [],
    "cwd": "/home/guest",
    "prompt": "guest@lucas-server:~$"
  },
  "snapshotPatch": {
    "terminal.lastCommand": "cat ./sys/temp/Memory_Dump_082.tmp"
  }
}
```

---

## 8-6. 존재하지 않는 파일

### 입력

```bash
cat worlld_map.map
```

### 처리 결과

```json
{
  "transitionResult": "STAY",
  "nodeCode": "CH2_FILE_LIST",
  "terminalResult": {
    "stdout": [],
    "stderr": [
      "cat: worlld_map.map: No such file or directory"
    ],
    "cwd": "/home/guest",
    "prompt": "guest@lucas-server:~$"
  },
  "snapshotPatch": {
    "terminal.lastCommand": "cat worlld_map.map"
  }
}
```

---

## 9. TerminalCommandService 처리 흐름

## 9-1. 전체 흐름

```
POST /api/v1/story/transitions
↓
active tab 검증
↓
snapshotVersion 검증
↓
user_story_progress 조회
↓
latest_snapshot_json 로드
↓
chapter02-vfs.json 로드
↓
effectiveVfs 생성
  baseVfsJson + vfsOverlay.createdNodes - vfsOverlay.removedPaths + vfsOverlay.modifiedNodes
↓
CommandParser
↓
PathResolver
↓
위험 명령어 검사
↓
StoryTransition 매칭 우선 시도
↓
결과 분기
  - 스토리 진행 명령이면 MOVE
  - 일반 탐색 명령이면 TerminalCommandService가 STAY 처리
  - 보호 코어 포함 tar면 CH2_PROTECTED_CORE_DENIED
  - 위험 명령이면 STAY + DENIED
  - 순서 오류면 STAY + WAIT
  - GC 100% 도달이면 CH2_FAIL_GC_TRACE_COMPLETE
↓
user_story_progress.latest_snapshot_json 갱신
↓
Redis story:context 갱신
↓
Redis story:recent-actions 갱신
↓
응답 반환
```

---

## 9-2. Active Tab Redis key

```
active-tab:story:{userId}:{chapterCode}
```

동일 유저가 여러 탭에서 동시에 명령을 입력하는 경우 상태 충돌을 막기 위해 active tab 검증을 수행한다.

---

## 9-3. 최근 행동 Redis key

```
story:context:{userId}:{chapterCode}
story:recent-actions:{userId}:{chapterCode}
```

최근 행동 로그는 RDB에 저장하지 않고 Redis bounded queue/list로 관리한다.

---

## 9-4. snapshot 저장 정책

| 상황 | user_story_progress | save_slots |
| --- | --- | --- |
| 일반 탐색 명령 | 상태 변경이 있으면 갱신 | 저장하지 않음 |
| 스토리 전이 성공 | 갱신 | 체크포인트면 저장 가능 |
| 체크포인트 도달 | 갱신 | 저장 가능 |
| 수동 저장 | 갱신 유지 | 현재 snapshot 복사 |
| 게임오버 후 재시도 | 체크포인트 기준 복구 | 필요 시 참조 |

---

## 10. Chapter 02 완료 처리

`CH2_COMPLETE`에 도달하면 다음 상태를 갱신한다.

```
user_chapter_progress
- Chapter 2 status = COMPLETED
- completed_at = NOW()

user_chapter_progress
- Chapter 3 status = UNLOCKED

user_story_progress
- latest_chapter_id = Chapter 2
- latest_node_id = CH2_COMPLETE
- latest_checkpoint_node_id = CH2_COMPLETE
- latest_snapshot_json.flags.chapter2_completed = true
```

필요하다면 `user_fragments`에 다음 파편 획득 기록을 저장한다.

```
fragment_code = laplace_fragment_01
```

---

## 11. 커버리지 체크리스트

| 원문 요소 | 반영 위치 |
| --- | --- |
| Chapter 2 클릭 후 시작 | `POST /api/v1/story/start` → `CH2_SERVER_HOME` |
| 터미널에 `guest@lucas-server:~$` 프롬프트 | `CH2_SERVER_HOME` |
| `Lucas: "접속 성공이야!"` | `CH2_SERVER_HOME` |
| 루카스 개인 서버 설명 | `CH2_SERVER_HOME` |
| 파일을 확인해보라는 루카스 대사 | `CH2_SERVER_HOME` |
| `ls` 입력 | `CH2_SERVER_HOME` → `CH2_FILE_LIST` |
| 자유 `pwd`, `ls`, `cd`, `cat` 탐색 | `TerminalCommandService`, VFS, `latest_snapshot_json.terminal.cwd` |
| VFS JSON 관리 | `resources/story/chapter02/vfs.json` |
| 유저별 현재 디렉토리 저장 | `user_story_progress.latest_snapshot_json.terminal.cwd` |
| 세이브 시점 현재 디렉토리 저장 | `save_slots.snapshot_json.terminal.cwd` |
| `world_map.map` | VFS, `CH2_WORLD_MAP_VIEW` |
| `observer_status.log` | VFS, `CH2_OBSERVER_STATUS_VIEW` |
| `lucas_fragment_01.sh` | VFS, `CH2_LUCAS_FRAGMENT_VIEW`, `CH2_LUCAS_FRAGMENT_EXEC_FAILED` |
| `laplace_fragment_01.sh` | VFS, `CH2_LAPLACE_MISSION_READY` |
| `trash/` | VFS, `CH2_FILE_LIST` |
| `sys/temp/Memory_Dump_082.tmp` | VFS, `CH2_TMP_SEARCH_RESULT` |
| `cache/User_Behavior_88.tmp` | VFS, `CH2_TMP_SEARCH_RESULT` |
| `tmp/System_Temp_File.tmp` | VFS, `CH2_TMP_SEARCH_RESULT` |
| `root/hidden/L_fragment_core.tmp` | VFS, `CH2_TMP_SEARCH_RESULT`, `CH2_PROTECTED_CORE_DENIED` |
| `cat world_map.map` | `CH2_FILE_LIST` → `CH2_WORLD_MAP_VIEW` |
| `[SESSION MAP UPDATE]` | `CH2_WORLD_MAP_VIEW` |
| `[Deleted]`, `[Null]`, `[PLAYER_ZONE]` 지도 | `CH2_WORLD_MAP_VIEW` |
| `Boundary Stability` | `CH2_WORLD_MAP_VIEW` |
| 루카스의 바깥 세계 붕괴 설명 | `CH2_WORLD_MAP_VIEW` |
| `[SYSTEM_ALERT] 비정상적인 프로세스 점유 감지` | `CH2_GC_SCAN_ALERT` |
| `[SYSTEM_ALERT] 가비지 컬렉터 스캔 시작... 0.1% 완료` | `CH2_GC_SCAN_ALERT` |
| GC 스캔 바 1% | `CH2_GC_SCAN_ALERT` |
| `sh laplace_fragment_01.sh` | `CH2_GC_SCAN_ALERT` → `CH2_LAPLACE_MISSION_READY` |
| `[LAPLACE FRAGMENT 01 :: DECOY PACKET]` | `CH2_LAPLACE_MISSION_READY` |
| decoy packet 미션 안내 | `CH2_LAPLACE_MISSION_READY` |
| `find . -name "*.tmp"` | `CH2_LAPLACE_MISSION_READY` → `CH2_TMP_SEARCH_RESULT` |
| `.tmp` 파일 목록 | `CH2_TMP_SEARCH_RESULT` |
| 분석 표 | `CH2_TMP_SEARCH_RESULT` |
| 정상 tar 명령 | `CH2_TMP_SEARCH_RESULT` → `CH2_DECOY_CREATED` |
| `decoy.tar` 생성 상태 | `latest_snapshot_json.vfsOverlay.createdNodes` |
| 보호 코어 포함 tar 명령 | `CH2_TMP_SEARCH_RESULT` → `CH2_PROTECTED_CORE_DENIED` |
| 권한 거부 출력 | `CH2_PROTECTED_CORE_DENIED` |
| `nc -w 3 127.0.0.1 8080 < decoy.tar` | `CH2_DECOY_CREATED` → `CH2_DECOY_SENT` |
| GC 감시 프로세스 gate_04 이동 | `CH2_DECOY_SENT` |
| `rm decoy.tar && history -c` | `CH2_DECOY_SENT` → `CH2_TRACE_CLEANED` |
| `[CLEAN] 접속 기록 삭제 완료.` | `CH2_TRACE_CLEANED` |
| `decoy.tar` 삭제 상태 | `latest_snapshot_json.vfsOverlay.removedPaths` |
| 루카스 성공 대사 | `CH2_TRACE_CLEANED` |
| 복구 문서 조각 | `CH2_RECOVERED_DOCUMENT` |
| 넥서스 내부 문서 | `CH2_RECOVERED_DOCUMENT` |
| Chapter 2 완료 | `CH2_COMPLETE` |
| GC 스캔 100% 실패 | `CH2_FAIL_GC_TRACE_COMPLETE` |
| 게임 오버 후 재시도 | `CH2_FAIL_GC_TRACE_COMPLETE` |
| 위험한 명령어 처리 | `TerminalCommandService`의 `STAY + DENIED` |
| 순서 오류 명령어 처리 | `TerminalCommandService`의 `STAY + WAIT` |